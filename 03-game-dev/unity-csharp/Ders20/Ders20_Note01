namespace DersNotlari.Ders20
{
    public class Ders20Notes
    {
        // ? Bu derste bu zamana kadar ogrendigimiz onemli kavramlarin bir genel tekrarini yaptik!
        
        // @ GetComponent<>() ile FindAnyObjectByType<>() arasindaki fark nedir?
        // --> GetComponent<>() => Sadece bu sinifin yer aldigi game objectin icindeki componentlarda arama yapar!
        // --> FindAnyObjectByType<>() => Sahnede yer alan butun game objectlerin icinde verilen componenti arar!
        
        // $ SOLID PRENSIPLERI
        
        // --> Single Responsibility Principle (Tek Sorumluluk Prensibi)
        // --> Open/Closed Principle (Acik/Kapali Prensibi)
        // --> Liskov Substitution Principle (Liskov Yerine Gecme Prensibi)
        // --> Interface Segregation Principle (Arayuz Ayirma Prensibi)
        // --> Dependency Inversion Principle (Bagimliliklarin Tersine Cevrilmesi Prensibi)
        
        // ! Single Responsibility Principle (Tek Sorumluluk Prensibi)
        // ? Tek Sorumluluk Prensibi, bir sınıfın yalnızca bir tek sorumluluğu olması gerektiğini belirtir. Bu prensip, bir sınıfın yalnızca bir iş yapması ve bu işten başka bir şeye karışmaması gerektiğini savunur.
        // ? Bu prensip, sınıfların daha modüler ve anlaşılır olmasını sağlar.
        // ? Örneğin, bir kullanıcı yönetim sistemi geliştiriyorsanız, kullanıcı verilerini yönetmek için bir sınıf, kullanıcı arayüzünü yönetmek için başka bir sınıf oluşturmalısınız.
        // ? Bu sayede, değişiklik yapmanız gerektiğinde yalnızca ilgili sınıfta değişiklik yapmanız yeterli olur, diğer sınıflar etkilenmez.
        
        // ! Open/Closed Principle (Acik/Kapali Prensibi)
        // ? Açık/Kapalı Prensibi, yazılım varlıklarının (sınıflar, modüller, fonksiyonlar vb.) genişletmeye açık, ancak değişime kapalı olması gerektiğini belirtir.
        // ? Bu prensip, yeni davranışların eklenebilmesi için kodun değiştirilmesi yerine genişletilmesi gerektiğini savunur.
        // ? Örneğin, bir ödeme sistemi geliştiriyorsanız ve farklı ödeme yöntemleri eklemek istiyorsanız, mevcut kodu değiştirmek yerine yeni ödeme yöntemleri ekleyerek sistemi genişletebilirsiniz.
        
        // ! Liskov Substitution Principle (Liskov Yerine Gecme Prensibi)
        // ? Liskov Yerine Geçme Prensibi, türetilmiş sınıfların, temel sınıfların yerine kullanılabilmesi gerektiğini belirtir.
        // ? Bu prensip, alt sınıfların, üst sınıfların yerine geçecek şekilde tasarlanmasını ve bu şekilde kullanılabilmesini sağlar.
        // ? Örneğin, bir geometrik şekiller uygulaması geliştiriyorsanız, tüm şekillerin bir Shape sınıfından türetilmiş olması ve her bir şeklin draw metodunu implemente etmesi gerekir.
        
        // ! Interface Segregation Principle (Arayuz Ayirma Prensibi)
        // ? Arayüz Ayırma Prensibi, bir sınıfın, kullanmadığı metotları içeren arayüzleri implemente etmemesi gerektiğini belirtir.
        // ? Bu prensip, büyük ve genel arayüzler yerine, daha küçük ve spesifik arayüzlerin kullanılmasını savunur.
        // ? Örneğin, bir hayvanat bahçesi uygulaması geliştiriyorsanız, her hayvan için genel bir Animal arayüzü yerine, farklı özelliklere sahip hayvanlar için spesifik arayüzler oluşturmalısınız.
        
        // ! Dependency Inversion Principle (Bagimliliklarin Tersine Cevrilmesi Prensibi)
        // ? Bağımlılıkların Tersine Çevrilmesi Prensibi, üst seviye modüllerin, alt seviye modüllere bağımlı olmaması gerektiğini belirtir. Bunun yerine, her iki tür modül de soyutlamalara bağımlı olmalıdır.
        // ? Bu prensip, bağımlılıkların daha esnek ve sürdürülebilir olmasını sağlar.
        // ? Örneğin, bir bildirim sistemi geliştiriyorsanız, bildirim gönderen sınıfın doğrudan e-posta veya SMS sınıflarına bağımlı olmaması, bunun yerine bir Notifier arayüzüne bağımlı olması gerekir.
        
        
        /*
         * SOLID Prensipleri, yazılım geliştirme süreçlerinde kaliteyi artırmak ve sürdürülebilirliği sağlamak için büyük önem taşır.
         * Tek Sorumluluk Prensibi, sınıfların tek bir sorumluluğu olması gerektiğini belirtirken, Açık/Kapalı Prensibi, yazılım varlıklarının genişletmeye açık, ancak değişime kapalı olması gerektiğini savunur.
         * Liskov Yerine Geçme Prensibi, türetilmiş sınıfların temel sınıfların yerine kullanılabilmesini sağlarken, Arayüz Ayırma Prensibi, sınıfların kullanmadığı metotları içeren arayüzleri implemente etmemesi gerektiğini belirtir.
         * Bağımlılıkların Tersine Çevrilmesi Prensibi ise üst seviye modüllerin, alt seviye modüllere bağımlı olmamasını, her iki tür modülün de soyutlamalara bağımlı olmasını savunur.
         * SOLID Prensipleri'ni doğru bir şekilde uygulayarak, esnek, yeniden kullanılabilir ve bakımının kolay olduğu yazılımlar geliştirebilirsiniz.
         * Bu prensipler, yazılım geliştirme süreçlerinde karşılaşılan birçok sorunu çözmeye yardımcı olur ve yazılımın uzun vadede sürdürülebilir olmasını sağlar.
         */
        
        // $ Unity'nin kendi SOLID Presnipleri ve Design Pattern'lar ile ilgili bir PDF kaynagi var. Indirdim. Ona bakabilirsin !
        
        
    }
}